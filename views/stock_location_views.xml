<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_location_form_inherit_notification" model="ir.ui.view">
        <field name="name">stock.location.form.inherit.notification</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@name='additional_info']" position="after">
                <group string="Responsible Users &amp; Notifications" name="location_responsibles">
                    <field name="responsible_user_ids" widget="many2many_tags" options="{'no_create': True}"/>
                </group>
            </xpath>
        </field>
    </record>
</odoo>
